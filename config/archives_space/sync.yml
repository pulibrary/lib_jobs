---
protocol: <%= ENV["SYNC_ASPACE_PROTOCOL"] || 'http' %>
host: <%= ENV["SYNC_ASPACE_HOST"] || 'localhost' %>
port: <%= ENV["SYNC_ASPACE_PORT"] || '8089' %>
path: <%= ENV["SYNC_ASPACE_PATH"] || '/' %>

username: <%= ENV["SYNC_ASPACE_USER"] || 'admin' %>
password: <%= ENV["SYNC_ASPACE_PASSWORD"] || 'secret' %>

page_size: <%= ENV["SYNC_ASPACE_PAGE_SIZE"] || '50' %>
throttle: <%= ENV["SYNC_ASPACE_THROTTLE"] || '0' %>
verify_ssl: <%= ENV["SYNC_ASPACE_VERIFY_SSL"] && ENV["SYNC_ASPACE_VERIFY_SSL"].downcase == 'true' || false %>

base_uri: <%= ENV["SYNC_ASPACE_URL"] %>
